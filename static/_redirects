# Redirect ONLY traffic from Netlify subdomain to your custom domain
# Prevent infinite loop
/*    https://chatat.org/:splat 301!    Host=chatat1.netlify.app

# Keep CMS working
/admin/*    /admin/index.html 200
/.netlify/identity/*  /.netlify/identity/:splat 200
